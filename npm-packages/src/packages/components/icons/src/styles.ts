import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  core: {
    margin: 0,
    padding: 0,
    textAlign: 'center',
    verticalAlign: 'middle',
    userSelect: 'none',
    fontFamily: 'Icons',
  },
});