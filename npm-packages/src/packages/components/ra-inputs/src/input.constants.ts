/**
 * @ Author: Redon Alla
 * @ Modified by: Redon Alla
 * @ Description: This file defines a series of constants that are likely used to standardize specific styles across a user interface, such as colors, sizes, and spacing. These constants can be used throughout the application to ensure consistency and make maintenance easier.
 */

/**
 * Constant representing the color white, typically used for text.
*/
export const WHITE_TEXT_COLOR = '#FFFFFF';

/** Multiplier for vertical padding, to be used for calculating dynamic padding values. */
export const PADDING_VERTICAL_MULTIPLIER = 0.5;

/** Multiplier for horizontal padding, applied to determine padding horizontally. */
export const PADDING_HORIZONTAL_MULTIPLIER = 0.75;

/** Scaling factor for icons to adjust their size dynamically. */
export const ICON_MULTIPLIER = 1.5;

/** Specific scaling factor for material design icons. */
export const MAT_ICON_MULTIPLIER = 2.3;

/** Specifies the width of a separator between icons. */
export const ICON_SEPARATOR_WIDTH = 1;

/** Multiplier for setting the bottom margin of labels. */
export const LABEL_MARGIN_BOTTOM_MULTIPLIER = 0.4;

/** Factor used to determine the size of checkbox containers or similar elements. */
export const CHECK_CONTAINER_SIZE_MULTIPLIER = 1.5;

/** Scaling multiplier for action container sizes in non-material settings. */
export const ACTION_CONTAINER_SIZE_MULTIPLIER = 1.4;

/** Larger scaling factor for action containers following material design guidelines. */
export const ACTION_CONTAINER_SIZE_MATERIAL_MULTIPLIER = 2.3;

/**
 * Multiplier that scales helper text, which provides supplementary information.
*/
export const TEXT_HELPER_MULTIPLIER = 0.78;

/** Controls the top margin of helper text, adjusting spacing above it. */
export const TEXT_HELPER_MARGIN_TOP_MULTIPLIER = 0.25;

/** Multiplier to set shadow opacity, affecting how dark shadows appear. */
export const SHADOW_OPACITY_MULTIPLIER = 0.5;

/** Multiplier to determine the blur radius of shadows, influencing shadow diffusion. */
export const SHADOW_RADIUS_MULTIPLIER = 0.5;