## Installation

To use the **{props.title}** you need to download packages in your local storage, and then you must install the packages with npm locally.
For more details on how to install packages locally [fallow this link](./getting-started/installation.mdx).

Installing packages using npm:


import CodeBlock from '@theme/CodeBlock';

<CodeBlock language="bash">{`npm install C:/ReactNative/framework/${props.name}`}</CodeBlock>