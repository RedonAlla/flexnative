**{props.name}** is part of the [**@flexnative/inputs**](./) package.
To use the **{props.name}** you needs to install `@flexnative/inputs` package.
[Fallow this link](../../components/inputs/index.mdx#installation) on how to install the package.